# Oikos vs Wings

Status: **pending manual verification**.

Run both daemons on the same VM, same Go/runtime workload, same number and type
of server, and identical sample duration. Record RSS, CPU, command latency,
container startup time, and recovery time. No result is claimed until the
head-to-head run is performed.
